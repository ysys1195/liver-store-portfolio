# 画面遷移・ワイヤーフレーム

## 1. 画面遷移

```text
Basic認証
   |
   v
TOP
   |
   +-- 初回Disclaimer Modal
   |      `-- 確認後TOP閲覧
   |
   +-- 商品一覧
   |      `-- 商品詳細
   |             `-- カート追加
   |
   +-- TOPの商品カード
   |      `-- 商品詳細
   |
   `-- Header Cart
          `-- カート
                 `-- Demo Checkout
                        `-- TOP
```

## 2. 共通ヘッダー

```text
+------------------------------------------+
| YUI STORE / LIVER STORE            CART  |
+------------------------------------------+
```

- ロゴ: TOPへ遷移
- Cart: `/cart`
- DesktopではProductsへの導線を追加してよい
- Mobileではハンバーガーメニューを必須としない

## 3. 初回Disclaimer Modal

```text
+--------------------------------------+
| Portfolio Demo                       |
|                                      |
| 本サイトは採用選考用に制作した       |
| 非公式ポートフォリオです。           |
|                                      |
| ANYCOLOR株式会社・にじさんじ公式     |
| とは関係ありません。                 |
|                                      |
| 掲載商品はデモ用であり、             |
| 実際の購入・決済はできません。       |
|                                      |
|       [ 内容を確認しました ]         |
+--------------------------------------+
```

- 閉じるボタンは設けない
- 確認状態はsessionStorageで保持する

## 4. TOP

```text
+------------------------------------------+
| HEADER                                   |
+------------------------------------------+
| HERO                                     |
|                                          |
| 夜風ユイ                                 |
| NEW VOICE                                |
| 「お兄ちゃん、24時になっちゃうよ？」   |
| ¥1,000                                   |
| [ 商品を見る ]                           |
+------------------------------------------+
| NEW ITEMS                                |
| [card] [card] [card]                     |
| [ 商品をすべて見る ]                     |
+------------------------------------------+
| ABOUT LIVER                              |
| ライバー紹介                             |
+------------------------------------------+
| Unofficial Portfolio Demo                |
+------------------------------------------+
```

## 5. 商品一覧 `/products`

```text
+------------------------------------------+
| HEADER                                   |
+------------------------------------------+
| PRODUCTS                                 |
| [ALL] [VOICE] [GOODS] [SET]              |
|                                          |
| [販売中] [残りわずか] [SOLD OUT]         |
| [COMING SOON] [販売終了]                 |
+------------------------------------------+
```

商品状態を意図的に分散させ、UIの各状態を確認できるようにする。

## 6. 商品詳細 `/products/:slug`

Desktop:

```text
+------------------------------------------+
| HEADER                                   |
+------------------------------------------+
| [ IMAGE ]      CATEGORY                  |
|                商品名                    |
|                ¥1,000                    |
|                販売状態                  |
|                最新在庫 / 更新           |
|                数量 [-] 1 [+]            |
|                [ カートに追加 ]          |
+------------------------------------------+
| 商品説明                                 |
+------------------------------------------+
| 販売期間                                 |
+------------------------------------------+
```

Mobile:

```text
IMAGE
商品情報
最新在庫 / 更新
数量
CTA
商品説明
販売期間
```

### CTA状態

販売中:

```text
[ カートに追加 ]
```

Pending:

```text
[ 追加中... ]
```

販売前:

```text
[ 9/10 18:00 販売開始 ]
```

売り切れ:

```text
[ SOLD OUT ]
```

### 在庫表示状態

- 初回取得中はSkeletonとスクリーンリーダー向けLoading文言を表示する
- 取得成功時は最新の在庫数と販売状態、手動の「在庫を更新」を表示する
- 商品なし（404）と一時的な取得失敗を別の文言で表示する
- 自動Retry後も失敗した場合は「もう一度試す」で手動refetchできる
- stale状態で画面へ戻った場合はバックグラウンドで最新在庫を再取得する
- 再取得した在庫数・販売状態は販売状態バッジ、CTA、数量上限にも反映する
- 初回Loading中または最終取得失敗時はカート追加操作を表示しない

## 7. カート `/cart`

```text
+------------------------------------------+
| CART                                     |
|                                          |
| 商品A  ¥1,000  [-] 1 [+]  [削除]        |
| 商品B  ¥1,500  [-] 1 [+]  [削除]        |
|                                          |
| 小計 ¥2,500                              |
| [ 購入手続きへ ]                         |
|                                          |
| ※実際の決済は行われません               |
+------------------------------------------+
```

Empty State:

```text
カートは空です

[ 商品を見る ]
```

- カートはクライアントで安全にhydrateした後に表示する
- 商品詳細とカートの数量増加ボタンは、商品詳細表示時の在庫数に達した時点でdisabledにする
- 数量上限はZustand store側でも適用し、保存データ復元時の上限超過も補正する
- 商品詳細の追加可能数は在庫数からカート内の数量を差し引く。復元完了まで数量・追加操作を無効化し、残り0点では「在庫上限に達しています」と表示する。成功案内は実際に数量が増えた場合に表示する
- カートの表示価格・在庫は確定値ではなく、実注文時にはサーバーで再検証する

## 8. Demo Checkout `/checkout`

```text
+------------------------------------------+
| Portfolio Demo                           |
|                                          |
| ここまでで購入体験は終了です。           |
| 実際の注文・決済は行われません。         |
|                                          |
| [ TOPへ戻る ]                            |
+------------------------------------------+
```

## 9. Flash Sale Simulation `/demo/flash-sale`

```text
+------------------------------------------+
| FLASH SALE SIMULATION                    |
|                                          |
| Product: Birthday Goods                  |
| Initial Stock: 5                         |
| Concurrent Requests: 20                  |
|                                          |
| [ Reset ] [ Run Simulation ]             |
|                                          |
| Requests          20                     |
| Success            5                     |
| Conflict          13                     |
| Duplicate          2                     |
| Remaining Stock    0                     |
+------------------------------------------+
```
